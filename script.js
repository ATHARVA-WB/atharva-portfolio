const menu=document.querySelector('.menu-btn');const nav=document.querySelector('.nav');if(menu)menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const glow=document.querySelector('.cursor-glow');window.addEventListener('pointermove',e=>{if(glow){glow.style.cssText=`position:fixed;pointer-events:none;z-index:0;width:260px;height:260px;border-radius:50%;left:${e.clientX-130}px;top:${e.clientY-130}px;background:radial-gradient(circle,rgba(121,82,255,.09),transparent 65%);mix-blend-mode:screen`}});
